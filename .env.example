# Environment configuration (dev)
export ENVIRONMENT=dev

# Run browser in headless mode (any value = headless, empty = headed)
export HEADLESS=

# CI environment flag (set automatically in CI/CD pipelines)
export CI=
